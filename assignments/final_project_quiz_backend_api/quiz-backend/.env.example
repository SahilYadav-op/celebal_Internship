# copy this to .env and edit if needed

# default sqlite database (no setup required)
DATABASE_URL=sqlite:///./quiz.db

# for postgres use something like:
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/quiz

APP_NAME=Quiz Backend Management API
